1.线程间的通讯
简单消费者和生产者之间的模型。

1.wait()并且不占用CPU资源。
2.生产者把盘子的里的东西做足了。满盘子了。为1.给食客吃光了为0.
厨师做满了，也该不占用CPU资源，等待食客吃光。
食客吃光也该不占用CPU资源，等待厨师制作满盘子的食物。

盘子吃光了，还是满盘子就是旗子。


wait/notify
任何一个对象都拥有一个线程等待池，
挂在同一个对象的线程等待池中的线程之间可以互相唤醒。
wait/notify方法是属于Object类的。

wait方法的使用必须放入synchronized同步块中。
先学会搭建模型：
